@AGENTS.md

# Reminder (enforced)
All work, code changes, and `pm2` / dev server runs MUST be in `/home/elijah/loan-app` only.
See the "CRITICAL: Main Tree Only Rule" section in AGENTS.md.
Never operate from or write to worktree paths.

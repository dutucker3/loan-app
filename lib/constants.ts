// lib/constants.ts
// Shared constants (no 'use server' to allow sync exports).
// Used for hierarchy root org, etc.

export const ROOT_ORG_NAME = 'Loan-App Platform';
export const ROOT_ORG_SLUG = 'loan-app-platform';
